# Vendored. MIT, so redistribution is granted outright.
#
# This upstream IS one skill: SKILL.md sits at the repo root rather than under
# skills/<name>/. The fetcher detects that shape and vendors the root as a
# single skill directory, so `include` trims it to the parts the skill needs.
kind: unit
name: terrashark
displayName: TerraShark
description: Terraform and infrastructure-as-code review - drift, blast radius, and state safety
category: infrastructure
tags: [terraform, iac, infrastructure, review]
attribution: Lukas Niessen (LukasNiessen)

upstream:
  repo: LukasNiessen/terrashark
  license: MIT
  vendor:
    from: .
    as: terrashark
    include: [SKILL.md, references, assets, docs, LICENSE, README.md]
